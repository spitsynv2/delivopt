# delivopt

Database configuration uses the defaults in `src/main/resources/config.properties`.
For a real database, provide `DB_URL`, `DB_USER`, and `DB_PASSWORD` as environment
variables instead of committing credentials.
